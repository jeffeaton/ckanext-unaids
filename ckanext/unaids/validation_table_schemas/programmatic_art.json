{
  "fields": [{
      "name": "facility_id",
      "title": "Facility ID",
      "type": "string",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "sex",
      "title": "Sex",
      "type": "string",
      "constraints": {
        "required": true,
        "enum": ["male", "female"]
      }
    }, {
      "name": "age_group_id",
      "title": "Age Group ID",
      "type": "integer",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "current_art",
      "title": "Number on ART",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 0
      }
    }, {
      "name": "new_clients",
      "title": "Number New Clients",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 0
      }
    }, {
      "name": "died",
      "title": "Number Died",
      "type": "integer",
      "constraints": {
        "required": true,
        "minimum": 0
      }
    }, {
      "name": "disengaged",
      "title": "Number Disengaged",
      "type": "integer",
      "decimalChar": ".",
      "constraints": {
        "required": true,
        "minimum": 0
      }
    }
  ],
  "primaryKey": ["facility_id", "sex", "age_group_id"],
  "foreignKeys": [{
    "fields": "facility_id",
    "reference": {
      "resource": "unaids_geographic_facilities",
      "fields": "facility_id"
    }
  }],
  "custom-constraint": {
    "constraint": "on_art >= new_clients",
    "constraint": "on_art >= died",
    "constraint": "on_art >= disengaged"
  }
}
